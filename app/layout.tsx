import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Corgi Haven Kennel',
  description:
    'Our kennel specializes in Welsh Corgi puppies with excellent genes, high quality of life, and superior breeding standards. We provide full support after purchase. Contact us to find your perfect Corgi companion!',
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang='en'>
      <body className={inter.className}>{children}</body>
    </html>
  );
}
